---
framework: jest
tier: small
postMerge:
  pattern:
    - "./tests/unit/chart/chart-spec.ts"
preMerge:
  pattern:
    - "./tests/unit/chart/chart-spec.ts"
preRun:
     command:
        - yarn install
postRun:
     command:
        - node --version
version: 2.1
